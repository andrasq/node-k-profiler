{
  "name": "k-profiler",
  "version": "0.2.0",
  "description": "kinvey convenience wrapper around v8-profiler",
  "main": "k-profiler.js",
  "keywords": ["v8-profiler", "profiler", "heapdump", "leak", "cpu"],
  "engines": {
    "node": ">= 0.10"
  },
  "license": "Kinvey",
  "repository": "git+ssh://github.com/andrasq/node-k-profiler",
  "dependencies": {
    "v8-profiler": "5.6.5"
  },
  "devDependencies": {
  },
  "scripts": {
    "test": "mocha --version && mocha -b test-k-profiler.js || echo 'mocha not installed; try \"npm install -g mocha\"'",
    "coverage": "nyc --version && nyc --include k-profiler.js mocha test-k-profiler.js || echo 'nyc not installed; try \"npm install -g nyc\"'"
  }
}
