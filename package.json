{
  "name": "@kinvey/profiler",
  "version": "0.5.2",
  "description": "kinvey convenience wrapper around v8-profiler",
  "main": "k-profiler.js",
  "keywords": ["v8", "profiler", "heapdump", "memory", "leak", "cpu", "usage"],
  "engines": {
    "node": ">= 0.10"
  },
  "license": "Kinvey",
  "repository": "git+ssh://github.com/andrasq/node-k-profiler",
  "dependencies": {
    "v8-profiler": "5.7.0"
  },
  "devDependencies": {
  },
  "scripts": {
    "test": "mocha --version && mocha -b test-k-profiler.js",
    "coverage": "nyc --version && nyc --include k-profiler.js mocha test-k-profiler.js || echo 'nyc not installed; try \"npm install -g nyc\"'"
  }
}
